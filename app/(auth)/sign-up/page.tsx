import { SignUpForm } from "@/components/auth/sign-up-form";

export const metadata = { title: "Create Account — Personal Finance" };

export default function SignUpPage() {
  return <SignUpForm />;
}
